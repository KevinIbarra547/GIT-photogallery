# Photo Gallery

This is a vanilla HTML/CSS/JavaScript project with no package dependencies or build step.

## Run

Serve the project root on Replit's preview port:

```sh
python3 -m http.server 5000 --bind 0.0.0.0
```

The main page is `index.html`. Edit it directly and refresh the preview to see changes.